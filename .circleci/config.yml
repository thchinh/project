# This config was automatically generated from your source code
# Stacks detected: deps:node:udagram-frontend
version: 2.1
orbs:
 docker: circleci/docker@1.5.0
jobs:
  build-test:
    description: "Pull code from github to Circle CI"
    executor: node/default # Install node dependencies and run tests
    steps:
      - checkout:
      - node/install-packages:
          pkg-manager: npm
      - run:
          name: Run tests
          command: npm run test

workflows:
  build-and-test: # name of workflow
    jobs:
      - build-test
      # - build-node:
      #     requires:
      #       - test-node
    # - deploy:
    #     requires:
    #       - build-node
